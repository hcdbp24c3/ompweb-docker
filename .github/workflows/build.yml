name: Auto-build Ompweb

on:
  schedule:
    # Every 6 hours
    - cron: "0 */6 * * *"
  workflow_dispatch:
    inputs:
      force:
        description: "Force rebuild even if versions unchanged"
        type: boolean
        default: false

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}   # ghcr.io/<owner>/ompweb-docker

jobs:
  check:
    name: Check for new versions
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.compare.outputs.changed }}
      ompweb_version: ${{ steps.versions.outputs.ompweb_version }}
      omp_version: ${{ steps.versions.outputs.omp_version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js (with npm cache)
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Fetch latest versions from npm
        id: versions
        run: |
          OMPWEB=$(npm view @kahme247/ompweb version)
          OMP=$(npm view @oh-my-pi/pi-coding-agent version)
          echo "ompweb_version=$OMPWEB" >> "$GITHUB_OUTPUT"
          echo "omp_version=$OMP" >> "$GITHUB_OUTPUT"
          echo "Latest ompweb: $OMPWEB"
          echo "Latest omp:    $OMP"

      - name: Compare with last built versions
        id: compare
        run: |
          FORCE="${{ github.event.inputs.force }}"
          OMPWEB="${{ steps.versions.outputs.ompweb_version }}"
          OMP="${{ steps.versions.outputs.omp_version }}"

          # Read last-built versions from committed VERSIONS file (if present)
          LAST_OMPWEB=""
          LAST_OMP=""
          if [ -f VERSIONS ]; then
            LAST_OMPWEB=$(grep -E '^OMPWEB=' VERSIONS | cut -d= -f2 || true)
            LAST_OMP=$(grep -E '^OMP=' VERSIONS | cut -d= -f2 || true)
          fi

          echo "Last built: ompweb=$LAST_OMPWEB, omp=$LAST_OMP"
          echo "Latest:     ompweb=$OMPWEB, omp=$OMP"

          if [ "$FORCE" = "true" ]; then
            echo "Force rebuild requested."
            echo "changed=true" >> "$GITHUB_OUTPUT"
          elif [ "$OMPWEB" != "$LAST_OMPWEB" ] || [ "$OMP" != "$LAST_OMP" ]; then
            echo "New version detected."
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "No version change. Skipping build."
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

  build:
    name: Build & push multi-arch image
    needs: check
    if: needs.check.outputs.changed == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write        # to commit updated VERSIONS file
      packages: write        # to push to GHCR
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=${{ needs.check.outputs.ompweb_version }}
            type=raw,value=latest

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            OMPWEB_VERSION=${{ needs.check.outputs.ompweb_version }}
            OMP_VERSION=${{ needs.check.outputs.omp_version }}
          # GitHub Actions cache: reuse layers between runs (free, per-repo)
          # mode=max caches ALL layers, not just the final one — so apt/nodejs/
          # bun/omp layers are reused even when the npm install layer changes.
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Record built versions
        run: |
          printf 'OMPWEB=%s\nOMP=%s\n' \
            "${{ needs.check.outputs.ompweb_version }}" \
            "${{ needs.check.outputs.omp_version }}" > VERSIONS
          cat VERSIONS

      - name: Commit updated VERSIONS
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: bump built versions to ompweb ${{ needs.check.outputs.ompweb_version }} / omp ${{ needs.check.outputs.omp_version }}"
          file_pattern: VERSIONS